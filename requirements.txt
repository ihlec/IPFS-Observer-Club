requests>=2.31
tomli>=2.0; python_version < "3.11"
fastapi>=0.110
uvicorn>=0.29
pynacl>=1.5
pypdf>=4.0
trafilatura>=1.8
# trafilatura imports lxml.html.clean, which moved to its own distribution
# in lxml 5. Without it the HTML extractor silently degrades.
lxml_html_clean>=0.1
pytest>=8.0
